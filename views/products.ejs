<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Tüm Ürünler Sayfası</h1>
    <div class="products">
      <% products.forEach(product => { %>
      <div><a href="/detail/<%= product.id %>"> <%= product.name %> </a></div>
      <% }) %>
    </div>
  </body>
</html>
